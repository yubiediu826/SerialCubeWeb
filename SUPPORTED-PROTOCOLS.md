# SerialCube 支持的协议类型

> **用途:** 嵌入式开发参考, 列 SerialCube 当前支持的协议类型与帧格式族.
> **数据保护范围:** BMS TLV v1 (`proto_bms`) 是**内部协议**, 字段细节不对外公开. Modbus RTU 是**公开标准**, 详细数据帧格式 / CRC 算法 / 读写举例可写. 9 种 `kind` 模板是 SerialCube 公开的字段模板, 可详细列出.

---

## 1. 内置默认协议 (2 个)

| 协议 ID | 名称 | 帧格式族 | 用途 | 公开度 |
|---------|------|----------|------|--------|
| `proto_bms` | BMS TLV v1 (Legacy) | fixed-header | 户外电源 / 储能 BMS 调试 | 🔒 内部协议, 字段细节不公开 |
| `proto_modbus` | Modbus RTU (Legacy) | fixed-header | 工业总线 Modbus RTU 调试 | 🌍 公开标准, 见 §2 详细 |

> 注: `proto_modbus` 当前无内置命令, 待真实用例出现后再补. 字段定义见 §2.2.

---

## 2. Modbus RTU 详细 (公开标准)

### 2.1 字段布局 (固定 7 段)

| 字段名 | 类型 | 字节数 | 字节序 | 默认值 | 说明 |
|--------|------|--------|--------|--------|------|
| `addr` | addr | 1 | - | `0x01` | 从站地址 (1-247, 0=广播) |
| `func` | cmd | 1 | - | `0x03` | 功能码 (0x01-0x10 等, 见 §2.3) |
| `reg_hi` | data | 1 | BE | `0x00` | 寄存器地址高字节 |
| `reg_lo` | data | 1 | BE | `0x00` | 寄存器地址低字节 |
| `qty_hi` | data | 1 | BE | `0x00` | 寄存器数量高字节 |
| `qty_lo` | data | 1 | BE | `0x00` | 寄存器数量低字节 |
| `crc` | crc | 2 | **LE** | `auto` | CRC16-Modbus 校验, 见 §2.4 |

**协议级配置:**
- 整帧字节序: `LE` (Little-Endian, Modbus RTU 标准)
- CRC 范围: `no_header` (CRC 不覆盖 header, 这里是 CRC 覆盖 addr+func+data)
- CRC 类型: `crc16-modbus`
- CRC 初始值: `0xFFFF`
- CRC 字节序: `LE` (低字节先发, Modbus RTU 标准)

### 2.2 通用请求帧格式 (主站 → 从站)

```
┌─────────┬─────────┬──────────┬──────────┬──────────┬──────────┬──────────┬──────────┐
│  addr   │  func   │ reg_hi   │ reg_lo   │ qty_hi   │ qty_lo   │ crc_lo   │ crc_hi   │
│ 1 byte  │ 1 byte  │ 1 byte   │ 1 byte   │ 1 byte   │ 1 byte   │ 1 byte   │ 1 byte   │
└─────────┴─────────┴──────────┴──────────┴──────────┴──────────┴──────────┴──────────┘
   0x01     0x03      0x00       0x00       0x00       0x0A       CRC16    CRC16
   (从站)   (读保持)  (寄存器 0x0000)        (读 10 个)          (低字节)  (高字节)
```

总长度: 8 字节 (固定)

### 2.3 常用功能码

| 功能码 | 名称 | 用途 | 备注 |
|--------|------|------|------|
| `0x01` | Read Coils | 读线圈 (DO) | 位操作 |
| `0x02` | Read Discrete Inputs | 读离散输入 (DI) | 位操作 |
| `0x03` | Read Holding Registers | 读保持寄存器 | **最常用**, 字操作 |
| `0x04` | Read Input Registers | 读输入寄存器 | 字操作 |
| `0x05` | Write Single Coil | 写单个线圈 | 位操作 |
| `0x06` | Write Single Register | 写单个寄存器 | 字操作 |
| `0x0F` | Write Multiple Coils | 写多个线圈 | 位操作 |
| `0x10` | Write Multiple Registers | 写多个寄存器 | 字操作 |

### 2.4 CRC16-Modbus 算法 (公开标准)

**算法步骤:**

1. 初始化 16 位 CRC 寄存器为 `0xFFFF`
2. 对帧的每个字节 (从 `addr` 到 `data` 最后一字节, **不含 CRC 本身**):
   - CRC = CRC XOR 字节
   - 重复 8 次: 如果 LSB=1, CRC = (CRC >> 1) XOR 0xA001; 否则 CRC = CRC >> 1
3. 输出 CRC, **低字节先发** (LE)

**多项式 (反射):** `0xA001` (即标准 `0x8005` 位反转)
**初始值:** `0xFFFF`
**不取反** (Modbus CRC16 不像 CRC-CCITT 那样最后 XOR 0x0000)

### 2.5 举例: 读保持寄存器 (0x03)

**主站请求** (读从站 0x01 寄存器 0x0000 - 0x0009 共 10 个):

| 字段 | addr | func | reg_hi | reg_lo | qty_hi | qty_lo | CRC |
|------|------|------|--------|--------|--------|--------|-----|
| 值   | 0x01 | 0x03 | 0x00   | 0x00   | 0x00   | 0x0A   | 计算 |

CRC16-Modbus 计算: 对 `[0x01, 0x03, 0x00, 0x00, 0x00, 0x0A]` 算 CRC = `0xC5CD` (LE 发: `0xCD 0xC5`)

完整请求字节: `01 03 00 00 00 0A CD C5`

**从站响应** (10 个寄存器 = 20 字节数据 + 1 字节 byte_count + 2 字节 CRC):

```
┌─────────┬─────────┬────────────┬─────────────────────┬──────────┬──────────┐
│  addr   │  func   │ byte_count │        data         │ crc_lo   │ crc_hi   │
│ 1 byte  │ 1 byte  │  1 byte    │   2N bytes (N 寄存器)│ 1 byte   │ 1 byte   │
└─────────┴─────────┴────────────┴─────────────────────┴──────────┴──────────┘
   0x01     0x03      0x14         [reg_0_hi, reg_0_lo,  CRC16     CRC16
   (从站)   (回 0x03) (20 字节数据) reg_1_hi, reg_1_lo,  (低字节)  (高字节)
                                   ...]
```

总长度: 3 + 2N + 2 字节

### 2.6 举例: 写单个寄存器 (0x06)

**主站请求** (写从站 0x01 寄存器 0x0001 = 0x0003):

| 字段 | addr | func | reg_hi | reg_lo | value_hi | value_lo | CRC |
|------|------|------|--------|--------|----------|----------|-----|
| 值   | 0x01 | 0x06 | 0x00   | 0x01   | 0x00     | 0x03     | 计算 |

完整请求字节: `01 06 00 01 00 03 [CRC lo] [CRC hi]`

**从站响应**: **回显请求** (8 字节, 同样格式)

---

## 3. 支持的 9 种帧格式族 (`kind`) — 详细字段

SerialCube 通过 `kind` 区分帧结构, 9 种内置模板可在"配置中心 → 协议"新建时选. **所有字段均可详细列出 (公开模板)**.

### 3.1 `fixed-header` — 通用固定头帧 (7 段)

| 字段名 | 类型 | 字节数 | 字节序 | 默认值 | 说明 |
|--------|------|--------|--------|--------|------|
| `header` | header | 1 | - | `0xAA` | 帧头, 用于帧定界 |
| `addr` | addr | 1 | - | `0x01` | 设备地址 |
| `cmd` | cmd | 1 | BE | `0x00` | 命令码 |
| `length` | length | 1 | BE | `auto` | data 段字节数, 编码时自动填 |
| `data` | data | 0 | BE | `0x00` | 数据段 (变长, 由命令 dataFields 决定) |
| `crc` | crc | 2 | LE | `auto` | CRC 校验 |
| `tail` | tail | 1 | - | `0x55` | 帧尾, 用于帧定界 |

总段数: 7, **帧头帧尾定界** (类似 HDLC/串口常见格式)

### 3.2 `raw` — 简化帧 (5 段)

| 字段名 | 类型 | 字节数 | 字节序 | 默认值 | 说明 |
|--------|------|--------|--------|--------|------|
| `header` | header | 1 | - | `0x5A` | 帧头 |
| `cmd` | cmd | 1 | BE | `0x00` | 命令码 |
| `length` | length | 1 | BE | `auto` | data 段字节数 |
| `data` | data | 0 | BE | `0x00` | 数据段 (变长) |
| `crc` | crc | 2 | LE | `auto` | CRC 校验 |

总段数: 5, **只有头, 无尾** (依赖 length + CRC 校验定界)

### 3.3 `cmd-split` — 命令独立帧 (4 段)

| 字段名 | 类型 | 字节数 | 字节序 | 默认值 | 说明 |
|--------|------|--------|--------|--------|------|
| `header` | header | 1 | - | `0xAA` | 帧头 |
| `cmd` | cmd | 1 | BE | `0x00` | 命令码 |
| `data` | data | 0 | BE | `0x00` | 数据段 (变长, 自带长度信息) |
| `crc` | crc | 2 | LE | `auto` | CRC 校验 |

总段数: 4, **无 length 段** (适合 data 段自带长度信息的协议)

### 3.4 `addr-split` — 多地址帧 (6 段)

| 字段名 | 类型 | 字节数 | 字节序 | 默认值 | 说明 |
|--------|------|--------|--------|--------|------|
| `header` | header | 1 | - | `0xAA` | 帧头 |
| `srcAddr` | data | 1 | BE | `0x01` | 源地址 |
| `dstAddr` | data | 1 | BE | `0x02` | 目标地址 |
| `cmd` | cmd | 1 | BE | `0x00` | 命令码 |
| `data` | data | 0 | BE | `0x00` | 数据段 (变长) |
| `crc` | crc | 2 | LE | `auto` | CRC 校验 |

总段数: 6, **源+目标双地址** (适合多设备互发, 如 DTU/RTU 中继场景)

### 3.5 `ctrl-bit7` — 控制位帧 (5 段)

| 字段名 | 类型 | 字节数 | 字节序 | 默认值 | 说明 |
|--------|------|--------|--------|--------|------|
| `header` | header | 1 | - | `0xAA` | 帧头 |
| `addr` | addr | 1 | - | `0x01` | 设备地址 |
| `ctrl` | data | 1 | BE | `0x00` | 控制字节 (高 7 位表命令, 低 1 位表方向/标志) |
| `data` | data | 0 | BE | `0x00` | 数据段 (变长) |
| `crc` | crc | 2 | LE | `auto` | CRC 校验 |

总段数: 5, **命令码藏在 ctrl 字节** (适合指令集小的协议)

### 3.6 `type-high-bit` — 类型高位帧 (5 段)

| 字段名 | 类型 | 字节数 | 字节序 | 默认值 | 说明 |
|--------|------|--------|--------|--------|------|
| `header` | header | 1 | - | `0xAA` | 帧头 |
| `addr` | addr | 1 | - | `0x01` | 设备地址 |
| `type` | data | 1 | BE | `0x00` | 类型字节 (高 bit 表命令类型) |
| `data` | data | 0 | BE | `0x00` | 数据段 (变长) |
| `crc` | crc | 2 | LE | `auto` | CRC 校验 |

总段数: 5, **type 字节高位区分命令类型** (适合自定义分类协议)

### 3.7 `msgid-mixed` — 消息 ID 混合帧 (4 段)

| 字段名 | 类型 | 字节数 | 字节序 | 默认值 | 说明 |
|--------|------|--------|--------|--------|------|
| `header` | header | 1 | - | `0xAA` | 帧头 |
| `msgID` | data | 2 | BE | `0x0000` | 消息 ID (2 字节) |
| `data` | data | 0 | BE | `0x00` | 数据段 (变长) |
| `crc` | crc | 2 | LE | `auto` | CRC 校验 |

总段数: 4, **消息 ID 替代 cmd** (适合有状态机/请求响应配对的协议)

### 3.8 `tlv` — TLV 编码帧 (3 段)

| 字段名 | 类型 | 字节数 | 字节序 | 默认值 | 说明 |
|--------|------|--------|--------|--------|------|
| `header` | header | 1 | - | `0xAA` | 帧头 |
| `tlv` | data | 0 | BE | `0x00` | TLV 段 (变长, Type-Length-Value 三元组) |
| `crc` | crc | 2 | LE | `auto` | CRC 校验 |

总段数: 3, **纯 TLV** (适合嵌套/可扩展协议, 如 SNMP/部分工业协议)

### 3.9 `custom` — 自定义帧 (0 段)

| 字段名 | 类型 | 字节数 | 字节序 | 默认值 | 说明 |
|--------|------|--------|--------|--------|------|
| (无) | - | - | - | - | 空白起步, 用户在配置中心自拼 |

总段数: 0, **完全自定义**

---

## 4. 字段类型 (Type) 说明

| type | 含义 | 序列化行为 |
|------|------|------------|
| `header` | 帧头 | 固定字节, 编码时填默认值 |
| `addr` | 设备地址 | 单字节, 0=广播 |
| `cmd` | 命令码 | 单字节, 区分命令 |
| `length` | 长度 | 1 字节, 编码时**自动计算** data 段长度填入 |
| `data` | 数据 | 变长, 按命令 `dataFields` 编码 |
| `crc` | 校验 | 2 字节, 编码时**自动计算** CRC 填入 |
| `tail` | 帧尾 | 固定字节, 编码时填默认值 |

**关键:** `length` 和 `crc` 字段编码时**自动填**, 不需要用户填, 但 `default` 仍需配置 (`auto` 表示自动).

---

## 5. CRC 类型支持

| CRC 类型 | 用途 | 算法公开度 |
|----------|------|-----------|
| `crc16-modbus` | Modbus RTU 协议标准 CRC16, **默认** | 🌍 公开标准, 见 §2.4 |
| (其他类型待扩展) | - | - |

---

## 6. 字节序

| 字节序 | 用途 | 说明 |
|--------|------|------|
| BE (大端) | 默认, 适用于多数 MCU 通信 | 字段级 + 协议级都默认 BE |
| LE (小端) | Modbus 等部分协议 | Modbus 整帧 LE, 但数据字段 (reg_hi/reg_lo) 是 BE |

> **注意:** SerialCube 协议级 `byteOrder` 跟字段级 `byteOrder` 是**两套**. 协议级是整帧的字节序, 字段级是字段内多字节的字节序. 如 `reg_hi` 和 `reg_lo` 字段都是 BE, 但 Modbus 整帧 LE.

---

## 7. 用户自定义协议

通过"配置中心 → 协议"新建, 流程:

1. 选 9 种 `kind` 之一作模板 (见 §3)
2. 编辑字段 (新增 / 删除 / 重命名 / 改类型 / 改字节数 / 改默认值)
3. 配协议级 `byteOrder` + `crcType` + `crcInit` + `crcEndian` + `crcRange`
4. 新建命令 (内嵌 `dataFields` 定义命令数据布局)
5. 应用协议 (1 协议同时只能有 1 个 active 协议)

---

## 8. 协议家族 (适用场景)

| 家族 | 典型协议 | SerialCube 支持方式 |
|------|----------|---------------------|
| 户外电源 | BMS (电池管理系统) | ✓ `proto_bms` 内置默认 (内部协议) |
| 户用储能 | EMS (能源管理) / BMS / PCS (储能变流器) | ✓ 自建协议 (9 kind 选) |
| 通信棒模块 | DTU / RTU | ✓ 自建协议 (推荐 `addr-split` 帧) |
| 工业总线 | Modbus RTU | ✓ `proto_modbus` 内置默认, 公开标准 |
| 通用串口设备 | 自定义协议 | ✓ `custom` kind 空白起步 |

---

## 9. 内部保密 (BMS TLV v1)

`proto_bms` 是 SerialCube 内部协议, 字段定义 / 命令集 / CRC 配置 / 解析逻辑**不对外公开**. 如需对真 BMS 设备调试, 请在 SerialCube 内"配置中心 → 协议"按设备说明书手动配 (推荐从 `fixed-header` 模板开始).

---

## 10. 关联文档

- 完整协议配置: SerialCube 配置中心 → 协议 Tab (UI 内)
- 9 种 `kind` 字段模板: SerialCube 内部 `NS.KIND_DEFAULTS` (本文 §3 已公开)
- 用户文档: [`README.md`](./README.md), [`docs/README.md`](./docs/README.md)
- 协议设计 spec: `docs/superpowers/specs/` 目录下的 v1.2.x spec (含协议层架构)
- Modbus 标准参考: <https://modbus.org/specs.php> (官方公开标准)
