# 2026-08-18 — v1.3.21 卡片系统基础（1×1/2×1 尺寸体系 + 5 新 type 颜色变量 + TREND 2×1 变体）

> **状态**: 已实施
> **作者**: Mavis (M3) + 用户 M.*
> **关联**:
> - Plan: `docs/superpowers/plans/2026-08-18-v1.3.21-card-system-and-detail-modal-implementation.md`
> - Spec: `docs/superpowers/specs/2026-08-18-card-system-and-detail-modal-design.md`
> - Preview: `docs/design/v1.3.21-card-system-and-detail-modal-overview.html`

## 一句话总结

卡片系统骨架 — 1×1 (220×198) 默认尺寸 + 2×1 (440×198) 横向 2 倍尺寸 (.card-default.span-2x1 变体) + 5 个新 type 颜色 token (--big 绿 / --array 蓝紫 / --string 灰 / --log 棕 / --wizard 青, 含 -soft 半透明 + 浅深主题双套) + TREND 卡片支持 `card.span='2x1'` 触发 2×1 变体 (左侧大字 + 右侧 sparkline 60s mini chart)。为后续 Task 3-5 (CTRL/BIT_SET/ARRAY 2×1) + Task 6 (chart-logs 弹窗复用) + Task 7-10 (BIG/STRING/LOG/WIZARD 弹窗) 铺底。

## 核心改动清单

| # | 文件:行号 | 改动 | 估行数 |
|---|---------|------|-------|
| 1 | SerialCube.html:9192 | VERSION 1.3.20 → 1.3.21 (manual bump, 脚本需交互) | 1 |
| 2 | SerialCube.html:8895 之后 | 插入 v1.3.21 changelog 段 (3 个 li: 尺寸体系 / 5 颜色变量 / TREND 2×1) | 8 |
| 3 | SerialCube.html:151 之后 (:root) | 5 新 type 颜色变量 (--big/--array/--string/--log/--wizard + -soft) | 5 |
| 4 | SerialCube.html:256 之后 (.theme-dark) | 同 5 变量暗色适配 (亮一档 + 16% soft 透明度) | 5 |
| 5 | SerialCube.html:6230 之后 | .card-default.span-2x1 基础尺寸 (width:440px + body padding + title/value/footer 字号) | 5 |
| 6 | SerialCube.html:6237 之后 | .card-default.trend.span-2x1 内部布局 (flex row + .left-col/.right-col + .mini-chart) | 4 |
| 7 | SerialCube.html:18153 之后 | renderCard trend 分支加 `if (card.span === '2x1')` 早期返回 (2×1 布局) | +30 |
| 8 | SerialCube.html:18200 之后 | setTimeout 调 NS.renderSparkline 在新 mini-chart canvas 上 (60s 复用) | +3 |

## 关键设计决策

### 1. 颜色变量体系: 浅深双套, 5 个新 type 各自独立 token

```css
:root {
  --big: #16a34a;       --big-soft: rgba(22, 163, 74, 0.14);     /* 绿 — BIG 卡片 (SOC/SOH/CycleCount) */
  --array: #5b21b6;     --array-soft: rgba(91, 33, 182, 0.12);   /* 蓝紫 — ARRAY 卡片 (Vcell[20] / Cell_Temp[8]) */
  --string: #67676c;    --string-soft: rgba(103, 103, 108, 0.14); /* 灰 — STRING 卡片 (设备名/序列号) */
  --log: #92400e;       --log-soft: rgba(146, 64, 14, 0.14);     /* 棕 — LOG 卡片 (历史日志) */
  --wizard: #0891b2;    --wizard-soft: rgba(8, 145, 178, 0.14);  /* 青 — WIZARD 卡片 (设置向导流程) */
}
.theme-dark {
  --big: #22c55e;       --big-soft: rgba(34, 197, 94, 0.16);
  --array: #a78bfa;     --array-soft: rgba(167, 139, 250, 0.14);
  --string: #98989f;    --string-soft: rgba(152, 152, 159, 0.16);
  --log: #f59e0b;       --log-soft: rgba(245, 158, 11, 0.16);
  --wizard: #22d3ee;    --wizard-soft: rgba(34, 211, 238, 0.16);
}
```

- **5 个 token 对应 Task 7-10 的 5 个新卡片类型** — Task 7-10 worker 直接用 `--big-soft` 给 BIG 卡片背景, `--array` 给 ARRAY 边框等
- **暗色主题用更亮色阶** (绿 #16a34a → #22c55e, 蓝紫 #5b21b6 → #a78bfa) — 配合暗背景提升对比度
- **-soft 透明度** 浅色 12-14% / 暗色 14-16% — 暗色背景纯度低, 软色块需要稍高不透明度

### 2. 尺寸体系: 1×1 默认 + 2×1 横向 2 倍, 通过 .span-2x1 class 触发

```css
.card-default.span-2x1 { width: 440px; }
.card-default.span-2x1 .card-body { padding: 10px 14px 8px; }
.card-default.span-2x1 .card-title { font-size: 0.8125rem; }  /* 0.6875 → 0.8125, 双倍空间大标题 */
.card-default.span-2x1 .card-value .num { font-size: 1.75rem; } /* 2 → 1.75, 不要喧宾夺主, 让 sparkline 也有空间 */
.card-default.span-2x1 .card-footer { font-size: 0.625rem; }
```

- **不用 grid span 而用 width** — SerialCube 的 `.card-grid-area` 走 flex (line 5914 `display: flex; flex-wrap: wrap;`), 显式 width 440px 最简单
- **2×1 卡片 body padding 加大** (10/14 vs 4/10) — 双倍空间给内部元素更多呼吸感
- **num 字号反而减小** (2rem → 1.75rem) — 2×1 卡片有 sparkline 占右侧, 数字不能跟 1×1 一样大, 否则视觉不均衡

### 3. TREND 2×1 内部布局: flex row, 左大字 + 右 sparkline

```css
.card-default.trend.span-2x1 .card-body { flex-direction: row; align-items: stretch; gap: 12px; }
.card-default.trend.span-2x1 .left-col { flex: 1; min-width: 0; display: flex; flex-direction: column; }
.card-default.trend.span-2x1 .right-col { flex: 1.2; min-width: 0; display: flex; flex-direction: column; gap: 4px; padding: 6px 0; }
.card-default.trend.span-2x1 .mini-chart { flex: 1; width: 100%; }
```

- **右列 flex 1.2** (vs 左列 1) — sparkline 区域稍宽, 容纳 60 个点 + 上下 padding
- **右列顶部 9px 缩写 "最近 60s"** — 跟 .card-status-badge 字号一致 (0.5625rem / 9px), uppercase 风格统一

### 4. renderCard trend 分支: 早返回避免 1×1 HTML 污染

```javascript
if (c.span === '2x1') {
  // 完全 2×1 HTML, 左侧 title+value+footer, 右侧 sparkline + "最近 60s" 标签
  card.innerHTML = `<div class="card-statusbar"></div>...`;
  return card;  // 早返回, 跳过底部 setTimeout 跟 1×1 渲染逻辑
}
```

- **早返回** 而不是 `else if` — 1×1 跟 2×1 渲染路径差异大, 不混在一起
- **canvas id 复用 `dh-trend-{c.id}`** — 复用现有 `NS.renderSparkline` 逻辑, 不重写

### 5. 协议守门: 0 行

```bash
git status --short -- docs/protocol/ docs/reference/
# 期望输出空
```

本次 8 处改动都在 `SerialCube.html` (UI/CSS/JS) + 文档 (CHANGELOG/README/HANDOVER), 不动 `docs/protocol/` + `docs/reference/`, 协议守门干净。

## 验证清单

- [x] VERSION 三处同步: SerialCube.html (const VERSION = '1.3.21') + README.md (v1.3.21) + docs/handover/PROJECT-HANDOVER-2026-08-11.md (v1.3.21)
- [x] HTML changelog 段插入: 8895 后 8 行 v1.3.21 段 (3 个 li)
- [x] docs/CHANGELOG.md 主索引行 +1 (v1.3.21 索引在 v1.3.20 之上, 倒序)
- [x] 5 新 type 颜色变量 (:root + .theme-dark 双套)
- [x] .card-default.span-2x1 + .card-default.trend.span-2x1 CSS
- [x] renderCard trend 分支支持 card.span='2x1'
- [x] 测试卡片 `NS.cards.push({ type: 'trend', field: 'Vcell_max', span: '2x1' })` 渲染 + 截图 + 删
- [x] 浏览器 console 无错
- [x] 协议守门 0 行
- [x] 3 个 commit (bump / CSS / TREND 2×1)

## 不在本次范围 (留给后续 worker)

- ❌ Task 3 CTRL 多 bit 卡片自动 2×1 (bitsCount > 8 触发)
- ❌ Task 4 BIT_SET 卡片强制 2×1 (16 bits 8×2 网格)
- ❌ Task 5 ARRAY 卡片强制 2×1 (4 区块 × 5 串 = 20 串)
- ❌ Task 6 __openDetail 弹窗改 chart-logs 复用
- ❌ Task 7-10 BIG / STRING / LOG / WIZARD 4 个新卡片类型 + 4 个新弹窗
- ❌ push (硬性: 等用户拍板)

## 关联 commit (待 worker 完成后回填)

- `chore(release): v1.3.20 → v1.3.21 (bump 准备)`
- `feat(v1.3.21): CSS 加 1×1/2×1 卡片尺寸体系 + 5 新 type 颜色变量`
- `feat(v1.3.21): TREND 卡片支持 2×1 变体 (card.span 字段)`
