# v1.3.18 — 串口日志断帧 + 默认 hex + 下拉框高度统一 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v1.3.18 一波实装 3 项设备连接管理改进 (组帧超时新功能 + 默认 hex RX/TX + 下拉框高度统一)

**Architecture:** 单 HTML 文件, 改动集中在 .config-panel 面板 + queueRxBytes 函数。CSS 1 行改 / state 默认值改 / 新加 1 个 HTML field + 1 个 state 字段 + 1 套 idle timer 逻辑。VERSION bump 单独 commit, 代码 commit 拆 2 个 (CSS+hex / timer)。

**Tech Stack:** 原生 HTML + CSS + JavaScript (vanilla, 无框架), localStorage 独立 key 持久化

## Global Constraints

- **commit message 中文** — 用户硬性要求, type ∈ {feat, fix, docs, chore, perf, refactor, test}
- **push 前必 ASK USER** — version-management R2 硬性规则
- **改 SerialCube.html 前必跑 bump-version.ps1** — version-management R1 硬性规则
- **协议守门** — 任何 git add 之前必跑 `git status --short -- docs/protocol/ docs/reference/` 期望 0 行
- **VERSION 三处同步** — SerialCube.html const VERSION / HTML changelog 段 / Git tag
- **commit 粒度** — VERSION bump 独立 commit, CSS+hex 1 commit, timer 1 commit, 文档 1 commit
- **bump-version.ps1 不读 env var** — `SC_AUTO_CONFIRM='y'` 无效, 改用手动 2 处 edit
- **agent-browser 跑 e2e 需 120s timeout** — 60s 默认不够
- **改 modal 后必 agent-browser 验证 + DOM 截图比对** — 不能只信 changelog
- **持久化 frameTimeout** — 新加独立 localStorage key `serialcube.frameTimeout`, 不动 _saveUserConfig
- **disconnect 集成** — disconnectSerial handler 必须 clearTimeout + finalize, 不丢尾巴字节
- **input 用 `change` 事件** — 失焦/回车才生效, 避免每键入重置 timer
- **不动协议解析器路径** — tryDispatchAckFrames 走自己 header/length/CRC, 不依赖 rxActiveEvent

---

## File Structure

| 文件 | 改动 | 行数估算 |
|------|------|----------|
| SerialCube.html | HTML + CSS + state + JS 改 | +60 / -5 |
| docs/README.md | 当前版本 + 最近 release | +1 / -1 |
| README.md | 当前版本 | +1 / -1 |
| docs/CHANGELOG.md | 索引 + 历史表 | +2 |
| docs/changelog/2026-08-17-v1.3.18-*.md | 新建 (子 changelog) | 新增 ~3KB |
| docs/handover/HANDOFF-V1.3.18-2026-08-17.md | 新建 (交接) | 新增 ~5KB |

---

## Task 1: 高度统一 CSS (1 行)

**Files:**
- Modify: `SerialCube.html:2285-2293` (.config-panel .field input/select padding)

**Interfaces:**
- Consumes: 现有 .config-panel .field input/select + .baud-rate-combo (line 2539-2594)
- Produces: 设备连接管理 5 字段同高 (波特率/数据位/停止位/校验位/组帧超时)

- [ ] **Step 1: 读现有 CSS**

读 `SerialCube.html:2280-2300`, 确认 .config-panel .field input/select 当前 padding (7px)。

- [ ] **Step 2: 改 padding 到 9px**

改 `.config-panel .field input` padding `7px 9px` → `9px 9px`
改 `.config-panel .field select` padding `7px 30px 7px 9px` → `9px 30px 9px 9px`

预期: 跟 .baud-rate-combo (line 2564 padding 9px 10px) 视觉同高。

- [ ] **Step 3: 自验**

```bash
git -c http.proxy= -c https.proxy= diff SerialCube.html | grep -A2 -B1 "config-panel .field"
```
预期: 看到 padding 7px → 9px 的 diff

- [ ] **Step 4: 暂不 commit (跟 Task 2 hex 默认一起 commit)**

## Task 2: 默认 hex RX/TX

**Files:**
- Modify: `SerialCube.html:9307` (state.settings.textEncoding 默认)
- Modify: `SerialCube.html:9309` (state.settings.sendMode 默认)
- Modify: `SerialCube.html:7946-7951` (text-encoding select 加 'hex' option)

**Interfaces:**
- Consumes: 现有 state.settings.textEncoding / sendMode 初始化 (line 9300-9320)
- Produces: 默认 hex (RX + TX), 用户可切

- [ ] **Step 1: 读现有默认值**

读 `SerialCube.html:9300-9320`, 确认 textEncoding + sendMode 当前默认值。

- [ ] **Step 2: 改 state 默认**

`SerialCube.html:9307`: `textEncoding: 'utf-8'` → `textEncoding: 'hex'`
`SerialCube.html:9309`: `sendMode: 'ascii'` → `sendMode: 'hex'`

- [ ] **Step 3: text-encoding select 加 'hex' option**

`SerialCube.html:7946-7951` 在 UTF-8 option 之前加 `<option value="hex">Hex</option>`, 注意 selected 默认属性。

- [ ] **Step 4: 自验**

```bash
git -c http.proxy= -c https.proxy= diff SerialCube.html | grep -E "textEncoding:|sendMode:|option value=\"hex\""
```
预期: 看到 3 处改

- [ ] **Step 5: 跟 Task 1 一起 commit**

```bash
git add SerialCube.html
git commit -m "feat(config): 设备连接管理 5 字段同高 + 默认 hex RX/TX" -m "- 高度统一: .config-panel .field input/select padding 7px → 9px, 跟 .baud-rate-combo 一致" -m "- 默认 hex: state.settings.textEncoding utf-8 → hex, sendMode ascii → hex" -m "- text-encoding select 加 hex 选项, 用户可切回 ASCII/UTF-8"
```

## Task 3: 组帧超时 HTML field + state + 持久化

**Files:**
- Modify: `SerialCube.html:7883` (parity 之后加 frame-timeout field)
- Modify: `SerialCube.html` (state.serial 加 _frameTimeoutId 字段 + state.settings.frameTimeout 默认 50)
- Modify: `SerialCube.html` (.config-grid CSS 4 col → 5 col)
- Modify: `SerialCube.html` (启动时从 localStorage 加载 frameTimeout)

**Interfaces:**
- Consumes: 现有 .config-grid 4 字段 (波特率/数据位/停止位/校验位)
- Produces: 5 字段 (加组帧超时), state.settings.frameTimeout, state.serial._frameTimeoutId

- [ ] **Step 1: 读现有 .config-grid 容器**

读 `SerialCube.html:7839` (.config-grid 容器定义) 找 CSS 改 grid-template-columns 处。

- [ ] **Step 2: HTML 加 frame-timeout field**

`SerialCube.html:7883` parity 闭合 div 之后, 加:

```html
<div class="field">
  <label for="frame-timeout">组帧超时</label>
  <input id="frame-timeout" type="number" min="0" max="2000" step="10" value="50">
</div>
```

- [ ] **Step 3: CSS .config-grid 4 col → 5 col**

找 .config-grid CSS 改 grid-template-columns 加 1 col。检查后, 现有 4 col 可能是 `grid-template-columns: repeat(4, 1fr)` 之类, 改成 5 col。

- [ ] **Step 4: state 加字段**

在 `SerialCube.html:9281` 附近 (state.serial) 加 `_frameTimeoutId: null`。
在 `SerialCube.html:9307` 附近 (state.settings) 加 `frameTimeout: 50`。

- [ ] **Step 5: 启动时从 localStorage 加载 frameTimeout**

找 `SerialCube.html` 启动初始化处 (init 函数 / DOMContentLoaded), 加:
```javascript
try {
  const stored = localStorage.getItem('serialcube.frameTimeout');
  if (stored !== null) {
    const n = parseInt(stored, 10);
    if (!Number.isNaN(n) && n >= 0 && n <= 2000) state.settings.frameTimeout = n;
  }
} catch (e) {}
```

- [ ] **Step 6: input 字段初值同步**

启动时设 `document.getElementById('frame-timeout').value = String(state.settings.frameTimeout)`。

- [ ] **Step 7: 自验**

```bash
git -c http.proxy= -c https.proxy= diff SerialCube.html | grep -E "frame-timeout|frameTimeout|grid-template-columns"
```
预期: 看到 HTML field + state + CSS 改

- [ ] **Step 8: 暂不 commit (跟 Task 4 timer 一起 commit)**

## Task 4: 组帧超时 JS timer + disconnect 集成

**Files:**
- Modify: `SerialCube.html:24177-24248` (queueRxBytes 加 timer)
- Modify: `SerialCube.html:~24123` (disconnectSerial 加 clearTimeout + finalize)
- Modify: `SerialCube.html` (input `change` 事件 handler)

**Interfaces:**
- Consumes: state.settings.frameTimeout, state.serial._frameTimeoutId, state.serial.rxActiveEvent, finalizeRxActiveEvent (line 24304), disconnectSerial
- Produces: 空闲 N 毫秒无新字节 → finalize 当前 active event; disconnect 不丢尾巴

- [ ] **Step 1: 读 queueRxBytes 全文**

读 `SerialCube.html:24177-24248` 了解现有 upsertLine + finalizeLine + 末尾 trimLiveEvents 流程。

- [ ] **Step 2: 加 helper 函数**

在 queueRxBytes 之后 (line 24248 之后) 加:

```javascript
function scheduleFrameTimeout() {
  if (state.settings.frameTimeout > 0 && state.serial.rxActiveEvent) {
    clearTimeout(state.serial._frameTimeoutId);
    state.serial._frameTimeoutId = setTimeout(flushFrameByTimeout, state.settings.frameTimeout);
  }
}
function flushFrameByTimeout() {
  state.serial._frameTimeoutId = null;
  if (state.serial.rxActiveEvent) {
    finalizeRxActiveEvent(nowPreciseMs(), { processRules: true, syntheticSplit: true });
  }
}
```

- [ ] **Step 3: queueRxBytes 末尾调 schedule**

`SerialCube.html:24237` (trimLiveEvents 之后, scheduleRefresh 之前) 加 `scheduleFrameTimeout();`

- [ ] **Step 4: disconnect 集成**

`SerialCube.html:~24123` (disconnectSerial 函数) 加:
```javascript
clearTimeout(state.serial._frameTimeoutId);
state.serial._frameTimeoutId = null;
if (state.serial.rxActiveEvent) {
  finalizeRxActiveEvent(nowPreciseMs(), { processRules: true, syntheticSplit: true });
}
```

注意: disconnect 路径已有类似 finalize 逻辑, 检查是否重复, 避免双重 finalize。

- [ ] **Step 5: input change 事件**

在 SerialCube.html 启动 binding 区 (找 existing input event handler for baud-rate 等参考) 加:
```javascript
const frameTimeoutInput = document.getElementById('frame-timeout');
if (frameTimeoutInput) {
  frameTimeoutInput.addEventListener('change', () => {
    const n = parseInt(frameTimeoutInput.value, 10);
    if (Number.isNaN(n) || n < 0 || n > 2000) {
      frameTimeoutInput.value = String(state.settings.frameTimeout);
      NS.toast('组帧超时必须是 0-2000 之间的整数', 'warn');
      return;
    }
    state.settings.frameTimeout = n;
    try { localStorage.setItem('serialcube.frameTimeout', String(n)); } catch (e) {}
    clearTimeout(state.serial._frameTimeoutId);
    state.serial._frameTimeoutId = null;
    scheduleFrameTimeout();
    NS.toast && NS.toast('组帧超时已设为 ' + n + ' ms', 'success', 1200);
  });
}
```

- [ ] **Step 6: 自验**

```bash
git -c http.proxy= -c https.proxy= diff SerialCube.html | grep -E "scheduleFrameTimeout|flushFrameByTimeout|frameTimeout|disconnectSerial"
```
预期: 看到 helper 函数 + 调用点

- [ ] **Step 7: 协议守门 + 大括号平衡**

```bash
git -c http.proxy= -c https.proxy= status --short -- docs/protocol/ docs/reference/
```
预期: 0 行
```powershell
$content = Get-Content D:\WorkSpace\SerialCubeWeb\SerialCube.html -Raw
($content.ToCharArray() | Where-Object { $_ -eq '{' } | Measure-Object).Count - ($content.ToCharArray() | Where-Object { $_ -eq '}' } | Measure-Object).Count
```
预期: 0

- [ ] **Step 8: 跟 Task 3 一起 commit**

```bash
git add SerialCube.html
git commit -m "feat(serial): 设备连接管理加组帧超时 (默认 50ms / 0-2000ms)" -m "- 解决从机连续发无换行帧 (如 5A 01 01 02 00 00 91 1D) 时 log 挤在一块的问题" -m "- queueRxBytes 末尾加 idle setTimeout, 无新字节超时 finalize 当前 active event" -m "- 0 = 关 (保持当前行为)" -m "- disconnect 集成 clearTimeout + finalize 不丢尾巴字节" -m "- input change 事件持久化到 localStorage.serialcube.frameTimeout"
```

## Task 5: VERSION bump + 文档同步 + check-readme-sync

**Files:**
- Modify: `SerialCube.html:9058` (VERSION 1.3.17 → 1.3.18)
- Modify: `SerialCube.html:8785+` (changelog 段加 1.3.18)
- Modify: `README.md:6` (当前版本)
- Modify: `docs/README.md:9+11+21` (最新版本 + 最近 release + 当前版本)
- Modify: `docs/CHANGELOG.md:16+` (索引 + 历史表)
- Create: `docs/changelog/2026-08-17-v1.3.18-frame-timeout-hex-heights.md` (子 changelog)
- Create: `docs/handover/HANDOFF-V1.3.18-2026-08-17.md` (交接)

**Interfaces:**
- Consumes: 现有 v1.3.17 的 4 文件同步模式 (参考 HANDOFF-V1.3.16-2026-08-17.md)
- Produces: VERSION + README + CHANGELOG + 子 changelog + 交接全部同步

- [ ] **Step 1: 读现有 v1.3.17 文档结构作为模板**

读 `docs/handover/HANDOFF-V1.3.16-2026-08-17.md` 了解交接文档结构
读 `docs/changelog/2026-08-17-v1.3.16-new-edit-cmd-datafield-grouping.md` 了解子 changelog 模板

- [ ] **Step 2: VERSION bump (1.3.17 → 1.3.18)**

`SerialCube.html:9058`: `const VERSION = '1.3.17'` → `const VERSION = '1.3.18'`
`SerialCube.html:8785+`: changelog 段顶加:
```html
<div class="version-release">
  <strong>1.3.18 版本 · 26-8-17</strong>
  <ul>
    <li>新增: 设备连接管理面板加"组帧超时"字段 (默认 50ms / 0-2000ms), 解决从机无换行帧 log 挤一块</li>
    <li>优化: 默认 16 进制接收 + 16 进制发送 (text-encoding select 加 hex 选项, sendMode 默认 hex)</li>
    <li>优化: 设备连接管理 5 字段 (波特率/数据位/停止位/校验位/组帧超时) 同高 (跟 baud-rate-combo 一致)</li>
  </ul>
</div>
```

> 注: bump-version.ps1 不读 SC_AUTO_CONFIRM env var, 手动 2 处 edit (跟 v1.3.17 一样)

- [ ] **Step 3: 根 README 同步**

`README.md:6`: `v1.3.17` → `v1.3.18`

- [ ] **Step 4: docs/README 同步**

`docs/README.md:9`: 最新版本行更新
`docs/README.md:11`: 最近 release 行加 v1.3.17
`docs/README.md:21`: 当前版本更新

- [ ] **Step 5: docs/CHANGELOG 索引 + 历史表**

`docs/CHANGELOG.md:16`: 索引加 v1.3.18 一行
`docs/CHANGELOG.md:~105` (历史 release 表): 加 v1.3.18 一行

- [ ] **Step 6: 子 changelog**

新建 `docs/changelog/2026-08-17-v1.3.18-frame-timeout-hex-heights.md`, 跟 v1.3.16 子 changelog 同结构, 内容覆盖 3 项改动。

- [ ] **Step 7: 交接文档**

新建 `docs/handover/HANDOFF-V1.3.18-2026-08-17.md`, 跟 HANDOFF-V1.3.16-2026-08-17.md 同结构, 加 v1.3.17 关系段。

- [ ] **Step 8: check-readme-sync 验证**

```bash
pwsh -File D:\WorkSpace\SerialCubeWeb\.minimax\skills\version-management\scripts\check-readme-sync.ps1
```
预期: 4/4 OK (如果挂, 按 v1.3.17 模式修)

- [ ] **Step 9: 协议守门 + 大括号平衡 (同 Task 4 Step 7)**

- [ ] **Step 10: commit (1 个 commit 包含所有文档)**

```bash
git add SerialCube.html README.md docs/README.md docs/CHANGELOG.md docs/changelog/2026-08-17-v1.3.18-frame-timeout-hex-heights.md docs/handover/HANDOFF-V1.3.18-2026-08-17.md
git commit -m "docs(release): v1.3.18 串口日志断帧 + 默认 hex + 下拉框高度统一 文档" -m "- VERSION bump 1.3.17 → 1.3.18" -m "- README + CHANGELOG + 子 changelog + 交接全部同步"
```

---

## Self-Review

**1. Spec 覆盖检查:**

- [x] A.1 HTML field — Task 3 Step 2
- [x] A.2 state + 持久化 — Task 3 Step 4-5
- [x] A.3 JS timer — Task 4 Step 2-3
- [x] A.4 disconnect 集成 — Task 4 Step 4
- [x] A.4 input change 事件 — Task 4 Step 5
- [x] B.1 state defaults — Task 2 Step 2
- [x] B.1 text-encoding option — Task 2 Step 3
- [x] C.1 CSS — Task 1
- [x] VERSION + 文档 — Task 5

**2. Placeholder 扫描:** 无 TBD/TODO, 所有 step 有具体文件 + 行号 + 代码

**3. 类型一致性:**
- `state.settings.frameTimeout` 一致 (Task 3 init + Task 4 读取)
- `state.serial._frameTimeoutId` 一致 (Task 3 init + Task 4 helper + Task 4 disconnect)
- `scheduleFrameTimeout` / `flushFrameByTimeout` 一致 (Task 4 Step 2 定义 + Step 3 调 + Step 5 调)
- `localStorage.serialcube.frameTimeout` 一致 (Task 3 Step 5 读 + Task 4 Step 5 写)

**4. 全局约束覆盖:**
- 中文 commit ✓
- 协议守门 step ✓
- 大括号平衡 step ✓
- VERSION 三处同步 ✓
- bump-version 手动 (脚本不读 env) ✓
- 文档 1 commit ✓

**5. 实施顺序合理:**
- Task 1+2: 纯改默认值/CSS, 无新逻辑
- Task 3: 新 HTML + state 字段
- Task 4: 新逻辑 (timer)
- Task 5: 文档
- 顺序: foundation → logic → docs, 合理
